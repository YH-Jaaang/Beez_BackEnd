<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.blockb.beez.dao.nonceCheckDao">
    <insert id="nonceCheck" parameterType="String">
        INSERT INTO NONCE (id, request_id, prefix)
        VALUES (NONCE_SEQ.NEXTVAL, #{nonceReqId}, #{prefix})
    </insert>
    <select id="nonceCount" resultType="Integer">
        SELECT COUNT(*) FROM NONCE WHERE status = 'pending'
    </select>
    <update id="nonceStausUpdate" parameterType="map">
        UPDATE NONCE SET used_nonce = #{usedNonce}, status = #{status} WHERE request_id = #{requestId} AND status = 'pending'
    </update>
</mapper>
