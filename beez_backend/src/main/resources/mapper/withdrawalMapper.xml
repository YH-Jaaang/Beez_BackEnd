<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.blockb.beez.dao.WithdrawalDao">
	<insert id="withdrawalHistory" parameterType="map">
		INSERT INTO WITHDRAW VALUES(WITHDRAW_SEQ.NEXTVAL, (SELECT user_id FROM MEMBER WHERE wallet_address = #{userAddress}), #{amount}, SYSDATE, #{txHash})
    </insert>
	<select id="withdrawHistoryList" resultType="com.blockb.beez.dto.WithdrawalHistoryDto">
		SELECT amount, tx_Hash txHash, withdraw_date withdrawDate
		FROM WITHDRAW
		WHERE user_id = #{userId}
		order by id desc
	</select>
</mapper>
